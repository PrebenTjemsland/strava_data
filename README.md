# React + TypeScript + Vite

This is a webapp made in react with vite and node.js for displaying stats and activities in a dashboard.

## Running code

Create an app on https://www.strava.com/settings/api (need a Strava user)
Fill in client id, client secrets and redirect uri in .env
Run npm i
Run node index.js
