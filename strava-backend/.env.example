STRAVA_CLIENT_ID=your_client_id
STRAVA_CLIENT_SECRET=your_client_secret
STRAVA_REDIRECT_URI=http://localhost:5050/api/exchange_token

# Optional: used if OAuth state is missing.
FRONTEND_REDIRECT_URI=http://localhost:5173

# Optional: backend listen port (defaults to 5050)
PORT=5050
